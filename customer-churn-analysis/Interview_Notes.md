Initially, I wanted to run a correlation matrix using a heatmap, however, it took long to load because of the high computation for the plotting.
Therefore I opted for segment analysis which is usually more useful and faster, since I wanted to answer the question: "Which customer groups churn more?"
During exploratory analysis, I kept churn as a categorical variable for interpretability, and only encoded it later during model preparation.
